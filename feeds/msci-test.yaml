name: msci-test
source:
  pattern: "data/msci_test/*.zip"   # the real drop
  members: "*"
parser:
  kind: plugin
  ref: msci-test
  options: {}
target:
  table: bronze.msci_test
policy:
  on_reject: quarantine
  max_reject_ratio: 0.01
