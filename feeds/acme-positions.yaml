name: acme-positions
source:
  pattern: "data/acme/*.zip"
  members: "*"
parser:
  kind: plugin
  ref: acme-positions
  options: {}
target:
  table: bronze.acme_positions
policy:
  on_reject: quarantine
  max_reject_ratio: 0.01
  workers: 8
